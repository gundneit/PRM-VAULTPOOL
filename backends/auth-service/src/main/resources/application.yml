spring:
  profiles:
    active: dev
    include: local
  application:
    name: la-vela-pool-booking
  jackson:
    default-property-inclusion: non_null
    serialization:
      write-dates-as-timestamps: false

server:
  port: 8080
  # Cho phép truy cập từ điện thoại / máy khác trong LAN (http://<IP_PC>:8080)
  address: 0.0.0.0
